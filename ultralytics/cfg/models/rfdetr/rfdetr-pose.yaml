# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Ultralytics RF-DETR pose / keypoint models with DINOv2 backbone
# Model docs: https://docs.ultralytics.com/models/rfdetr
# Task docs: https://docs.ultralytics.com/tasks/pose
# Note: imgsz comes from default.yaml / CLI / RFDETR(..., imgsz=...); not from this file.

# Parameters
nc: 90 # number of classes
scales: # size variants, i.e. 'model=rfdetr-pose-preview.yaml' loads this file with scale 'preview'
  preview: [] # RFDETRKeypointPreviewConfig — recommended imgsz=576 (must be ÷24)
